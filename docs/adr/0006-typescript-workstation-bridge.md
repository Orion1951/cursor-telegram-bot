# TypeScript for the MVP workstation bridge

Authoritative domain language stays technology-agnostic, yet the first workstation bridge will be implemented in TypeScript to reach working Telegram integrations quickly despite a strong .NET comfort baseline. The hypothesis is that prototype velocity and ecosystem examples matter more than runtime consolidation while the hybrid execution roadmap is still settling; if the bridge hardens into long-lived infrastructure we can revisit migrating or splitting services.
