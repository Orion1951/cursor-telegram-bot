# TypeScript strict mode from project start

The workstation bridge opts into full TypeScript `strict` (and preserves it as merge-default) despite early friction rewriting null-heavy Telegram update shapes—once Automated executor binding mutates workspaces, sloppy typing pays off far worse upstream. Loosening specific flags temporarily is reserved only for unblockers with expiry comments, never as permanent baseline ergonomics.
