# Vitest from the first commit

The workstation bridge adopts Vitest immediately so explicit operator pinning, Telegram update parsing, and manifest merge helpers gain regression harnesses while still in Acknowledgement-only posture—deferring tests until executor binding would let subtle adapter bugs compound. Node’s built-in test runner was rejected for MVP speed-of-iteration with TypeScript sources and watch ergonomics on the developer laptop.
