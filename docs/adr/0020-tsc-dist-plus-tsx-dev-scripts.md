# tsc emits dist while tsx speeds local dev loops

Stable workstation bridge artefacts come from **`tsc` compiling into `dist/`** so systemd-style daemons node directly against emitted ESM parity with Vitest-checked sources. Everyday hacking pairs that with **`tsx`-powered dev scripts** for instant reload ergonomics rather than bouncing full builds whenever Telegram polling scaffolding shifts—accepting the tiny drift risk Vitest guards against.
