# Zod-validated manifests

Session manifest parsing pairs **Zod** schemas with inferred TypeScript types so operator-facing catalogue edits fail fast inside Vitest-covered guards before malformed wiring reaches long polling loops. Lighter parsers were skipped because ergonomics authoring nested Session plus Dedicated steering bot descriptors outweighed marginal bundle sensitivity for this single-package workstation service.
